import 'package:flutter/material.dart';

class AdvisoryPage extends StatelessWidget {
  const AdvisoryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Advisory Page Content'),
    );
  }
}
